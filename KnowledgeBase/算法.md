# 算法

## 排序算法

### 手写快排

```C++
//快速排序
void quick_sort(int s[], int l, int r)
{
    if (l < r)
    {
        //Swap(s[l], s[(l + r) / 2]); //将中间的这个数和第一个数交换 参见注1
        int i = l, j = r, x = s[l];
        while (i < j)
        {
            while(i < j && s[j] >= x) // 从右向左找第一个小于x的数
                j--;
            if(i < j)
                s[i++] = s[j];

            while(i < j && s[i] < x) // 从左向右找第一个大于等于x的数
                i++;
            if(i < j)
                s[j--] = s[i];
        }
        s[i] = x;
        quick_sort(s, l, i - 1); // 递归调用
        quick_sort(s, i + 1, r);
    }
}
//快速排序
void quick_sort(int arr[], int L, int R){
  if(L>R){
     return;
  }
  //随机一个L到R的数，与R交换；swap(arr,L,R)是伪代码，交换arr中的L,R
  swap(arr,L+(int)(Math.random()*(R-L+1)),R)
  //border[]荷兰国旗问题相等的两边界
  int[]border=getBorder(arr,L,R);
  quick_sort(arr,L,border[0]-1);
  quick_sort(arr,border[1]+1,R);
}


int[] getBorder(int arr[], int L, int R){
  if(L==R){
      return new int[]{L,R};
  }
  int index=L;
  int pL=L-1;
  int pR=R;
  while(index<=pR){
      if(arr[index]==arr[R]){
          index++;
      }else if(arr[index]>arr[R]){
          swap(arr,index,--pR);
      }else{
          swap(arr,index++,++pL);
      }
  }
  swap(arr,pR,R);
  return new int[]{pL+1,pR};
}
```

### 快排在什么情况下时间复杂度会比较高

- 需要排序的序列与目标数组反置

## 查找算法

### 两个排序数组找中位数

### LRU算法的实现？

## 数据结构

### 链表插入时间复杂度

- O(n)

### 平衡二叉树的特点价值，查找效率

- 所有叶子节点高度差不超过1，log(n)

### 为什么使用红黑树

- AVL树的自旋比较耗时，需要折中自旋耗时以及最差情况的查询耗时，红黑树最长链不超过最短链的两倍

### avl树旋转调整原理，只需要分情况画图说明

## 算法题

### kmp

```C++
void get_next(string s,vector<int>& next){
    int len = s.size();
    next.resize(len);
    next[0] = -1;
    int j = 0;
    int k = -1;
    while(j<len - 1){
        if(k == -1 || s[j] == s[k]){
            if(s[++j] == s[++k]){
                next[j] = next[k];
            }
            else{
                next[j] = k;
            }
        }
        else{
            k = next[k];
        }
    }
}
```

### 背包问题

### 25匹马五个赛道比出前三的问题

### 求数组中每个位置下一个最大的数

- 单调栈

### 查找二叉树中第k小的元素？

- 中序遍历

### 求二叉树的层序遍历

https://www.nowcoder.com/practice/04a5560e43e24e9db4595865dc9c63a3?tpId=190&tqId=35337&rp=1&ru=%2Factivity%2Foj&qru=%2Fta%2Fjob-code-high-rd%2Fquestion-ranking&tab=answerKey

### 随机0,1产生均等的随机（0,n） 。。

### 10G数据，数据都是int32位，内存只有1G，如何去除重复元素

- Bitmap

## 算法参考资源

- https://blog.csdn.net/Code_star_one/article/details/109600676
