# C++面试知识点

> 按照重要程度排序，优先掌握⭐⭐⭐⭐⭐必考题

---

## 多态与虚函数

## 📌 C++多态机制（⭐⭐⭐⭐⭐ 必考）

### 🎯 得分关键词
> **虚函数表(vtable)** | **虚指针(vptr)** | **动态绑定** | **静态多态vs动态多态** | **重载vs重写** | **编译时vs运行时**

### ✅ 核心概念

C++多态分为两种：

**1️⃣ 静态多态（编译时多态）**
```cpp
// 函数重载 - 同名函数，不同参数
void print(int x);
void print(double x);
void print(string x);

// 编译时根据参数类型确定调用哪个函数
print(10);      // 调用 print(int)
print(3.14);    // 调用 print(double)
```

**2️⃣ 动态多态（运行时多态）**
```cpp
class Base {
public:
    virtual void show() { cout << "Base"; }  // 🔑 virtual关键字
};

class Derived : public Base {
public:
    void show() override { cout << "Derived"; }  // 🔑 重写虚函数
};

Base* ptr = new Derived();
ptr->show();  // 输出"Derived" - 运行时决定
```

### 💡 虚函数表实现原理（⭐⭐⭐⭐⭐ 必问）

**内存布局：**
```
┌─────────────────────────────┐
│    对象内存布局               │
├─────────────────────────────┤
│  vptr (虚指针)  ──────┐     │  ← 指向虚函数表
│  成员变量1             │     │
│  成员变量2             │     │
└────────────────────────┼────┘
                         │
                         ▼
              ┌──────────────────┐
              │   虚函数表(vtable) │
              ├──────────────────┤
              │ &Base::show      │
              │ &Base::func1     │
              │ &Base::func2     │
              └──────────────────┘
```

**调用过程：**
```cpp
Base* ptr = new Derived();
ptr->show();

// 底层实际执行：
// 1. 通过ptr找到对象
// 2. 读取对象的vptr
// 3. 通过vptr找到vtable
// 4. 在vtable中查找show()的地址
// 5. 调用该地址的函数
```

### 🔥 面试追问点

#### 1️⃣ 构造函数可以是虚函数吗？（⭐⭐⭐⭐⭐ 高频）

❌ **不可以**

**原因：**
```
1. 虚函数表在构造函数中初始化
2. 如果构造函数是虚函数，调用时找不到虚函数表
3. 逻辑矛盾：虚函数需要vptr，vptr在构造函数中才创建

【顺序】
对象创建 → 分配内存 → 调用构造函数 → 初始化vptr → vptr指向vtable
         ↑
      如果构造是虚函数，这里就找不到vtable了！
```

#### 2️⃣ 析构函数可以是虚函数吗？必须是吗？（⭐⭐⭐⭐⭐ 必问）

✅ **可以，而且基类析构函数必须是虚函数！**

**为什么必须：**
```cpp
class Base {
public:
    ~Base() { delete[] data; }  // ❌ 非虚析构
    int* data;
};

class Derived : public Base {
public:
    ~Derived() { delete[] moreData; }  // 子类析构
    int* moreData;
};

Base* ptr = new Derived();
delete ptr;  // ❌ 只调用~Base()，~Derived()不会被调用
             // 💣 内存泄漏！moreData没释放
```

**正确做法：**
```cpp
class Base {
public:
    virtual ~Base() { delete[] data; }  // ✅ 虚析构
};

delete ptr;  // ✅ 先调用~Derived()，再调用~Base()
```

🔑 **得分点：能说出内存泄漏的具体原因**

#### 3️⃣ 纯虚函数可以有实现吗？（⭐⭐⭐⭐）

✅ **可以有实现！**

```cpp
class Abstract {
public:
    virtual void func() = 0;  // 纯虚函数声明
};

// 纯虚函数也可以有实现
void Abstract::func() {
    cout << "纯虚函数的默认实现";
}

class Derived : public Abstract {
public:
    void func() override {
        Abstract::func();  // ✅ 可以调用基类的纯虚函数实现
        cout << "派生类的实现";
    }
};
```

**应用场景：**
- 提供默认实现，子类可选择性调用
- 强制子类必须重写，但提供公共逻辑

#### 4️⃣ 析构函数可以是纯虚函数吗？（⭐⭐⭐）

✅ **可以，但必须提供实现！**

```cpp
class Base {
public:
    virtual ~Base() = 0;  // 纯虚析构
};

// ✅ 必须提供实现，否则链接错误
Base::~Base() {
    cout << "Base析构";
}

// 原因：子类析构时会自动调用基类析构
// 如果基类析构没有实现，链接时会找不到符号
```

### 📊 重载 vs 重写 vs 隐藏

| 特性 | 重载(Overload) | 重写(Override) | 隐藏(Hide) |
|------|---------------|---------------|-----------|
| **发生位置** | 同一作用域 | 基类和派生类 | 基类和派生类 |
| **函数名** | 相同 | 相同 | 相同 |
| **参数列表** | 不同 | 相同 | 可同可不同 |
| **virtual** | 无关 | 基类必须有 | 基类无virtual |
| **绑定时机** | 编译时 | 运行时 | 编译时 |

```cpp
// 重载
void func(int);
void func(double);  // ✅ 重载

// 重写
class Base {
    virtual void func(int);
};
class Derived : public Base {
    void func(int) override;  // ✅ 重写
};

// 隐藏
class Base {
    void func(int);  // 非虚函数
};
class Derived : public Base {
    void func(int);  // ⚠️ 隐藏基类的func，不是重写
    void func(double);  // ⚠️ 同时隐藏基类的func
};
```

### 🎓 面试回答模板

```
【标准回答】（30秒版本）
C++多态分为静态多态和动态多态：

1. 静态多态（编译时）
   通过函数重载和模板实现，编译时确定调用哪个函数

2. 动态多态（运行时）
   通过虚函数和继承实现，运行时通过虚函数表动态绑定

   实现机制：
   - 每个包含虚函数的类都有一个虚函数表(vtable)
   - 每个对象包含一个虚指针(vptr)指向vtable
   - 调用虚函数时，通过vptr找到vtable，再找到函数地址

【追问-为什么构造不能是虚函数】
因为虚函数表在构造函数中初始化，如果构造函数本身是虚函数，
调用时找不到虚函数表，逻辑矛盾。

【追问-为什么析构要是虚函数】
如果基类析构不是虚函数，通过基类指针删除派生类对象时，
只会调用基类析构，派生类析构不会执行，造成内存泄漏。
```

### ⚠️ 常见误区

❌ **错误1**：认为有virtual就是多态
✅ **正确**：必须通过基类指针/引用调用，才能体现多态

❌ **错误2**：认为纯虚函数不能有实现
✅ **正确**：纯虚函数可以有实现，但类仍是抽象类

❌ **错误3**：以为重写和隐藏是一回事
✅ **正确**：重写需要virtual，隐藏不需要，行为完全不同

### 🌟 加分点

- 提到vptr在对象内存布局中的位置（通常在开头）
- 知道虚函数表是编译时确定的
- 了解虚函数的性能开销（一次间接调用）
- 提到final关键字（C++11，阻止继承/重写）

---

## 📌 智能指针（⭐⭐⭐⭐⭐ 必考）

### 🎯 得分关键词
> **RAII** | **引用计数** | **unique_ptr** | **shared_ptr** | **weak_ptr** | **循环引用** | **自动释放**

### ✅ 三种智能指针对比

| 智能指针 | 所有权 | 计数 | 使用场景 | 复制 |
|---------|--------|------|---------|------|
| **unique_ptr** | 独占 | 无 | 明确唯一所有权 | ❌ 不可复制，可移动 |
| **shared_ptr** | 共享 | 引用计数 | 多个所有者 | ✅ 可复制 |
| **weak_ptr** | 不拥有 | 弱引用 | 解决循环引用 | ✅ 可复制 |

### 💻 代码示例

**1️⃣ unique_ptr - 独占所有权**
```cpp
// ✅ 创建
unique_ptr<int> p1(new int(10));
auto p2 = make_unique<int>(20);  // 🌟 推荐方式（C++14）

// ❌ 不能复制
unique_ptr<int> p3 = p1;  // 编译错误

// ✅ 可以移动
unique_ptr<int> p4 = std::move(p1);  // p1变为nullptr
```

**2️⃣ shared_ptr - 共享所有权**
```cpp
// ✅ 创建
shared_ptr<int> p1(new int(10));
auto p2 = make_shared<int>(20);  // 🌟 推荐（性能更好，一次分配）

// ✅ 可以复制
shared_ptr<int> p3 = p1;  // 引用计数+1
cout << p1.use_count();   // 输出：2

// 作用域结束，自动释放
{
    shared_ptr<int> p4 = p1;  // 引用计数 = 3
}  // p4析构，引用计数 = 2
```

**3️⃣ weak_ptr - 解决循环引用**
```cpp
class B;
class A {
public:
    shared_ptr<B> ptrB;  // ❌ 如果B也有shared_ptr<A>，会循环引用
    ~A() { cout << "A析构"; }
};

class B {
public:
    weak_ptr<A> ptrA;    // ✅ 使用weak_ptr打破循环
    ~B() { cout << "B析构"; }
};

// 使用
shared_ptr<A> a(new A);
shared_ptr<B> b(new B);
a->ptrB = b;  // A持有B的shared_ptr
b->ptrA = a;  // B持有A的weak_ptr（不增加引用计数）

// a和b离开作用域时，都能正常析构
```

### 💡 实现原理（⭐⭐⭐⭐⭐ 高频追问）

**shared_ptr内部结构：**
```cpp
template<typename T>
class shared_ptr {
private:
    T* ptr;                    // 原始指针
    ControlBlock* control;     // 控制块
};

struct ControlBlock {
    int shared_count;   // 强引用计数（shared_ptr数量）
    int weak_count;     // 弱引用计数（weak_ptr数量）

    // 🔑 关键：两个计数都为0时，才删除控制块
};
```

**引用计数机制：**
```
初始：shared_count=1, weak_count=0
     ┌─────────┐
     │ object  │
     └─────────┘
          ↑
     ┌────┴────┐
     │  ptr    │
     │ control │──→ [shared:1, weak:0]
     └─────────┘
     shared_ptr

复制：shared_count=2, weak_count=0
     ┌─────────┐
     │ object  │
     └─────────┘
       ↑     ↑
       │     └────┐
     p1│         p2
```

### 🔥 面试追问点

#### 1️⃣ 为什么要用make_shared而不是new？（⭐⭐⭐⭐⭐ 必问）

```cpp
// ❌ 方式1：两次内存分配
shared_ptr<int> p1(new int(10));
// 分配1：new int(10) - 对象内存
// 分配2：控制块内存

// ✅ 方式2：一次内存分配
auto p2 = make_shared<int>(10);
// 🔑 对象和控制块一起分配，性能更好
```

**其他优点：**
- 异常安全
  ```cpp
  func(shared_ptr<int>(new int), may_throw());
  // ❌ 如果may_throw()先执行并抛异常，new int会泄漏

  func(make_shared<int>(10), may_throw());
  // ✅ 安全，要么全成功，要么全失败
  ```

#### 2️⃣ 循环引用是什么？怎么解决？（⭐⭐⭐⭐⭐ 必问）

**问题场景：**
```cpp
class Node {
public:
    shared_ptr<Node> next;
    shared_ptr<Node> prev;  // ❌ 双向链表的循环引用
};

shared_ptr<Node> n1(new Node);
shared_ptr<Node> n2(new Node);
n1->next = n2;  // n2的引用计数 = 2
n2->prev = n1;  // n1的引用计数 = 2

// n1和n2离开作用域
// n1的引用计数：2 → 1（仍>0，不释放）
// n2的引用计数：2 → 1（仍>0，不释放）
// 💣 内存泄漏！
```

**解决方案：**
```cpp
class Node {
public:
    shared_ptr<Node> next;
    weak_ptr<Node> prev;    // ✅ 用weak_ptr打破循环
};

// 现在n1和n2能正常释放
```

#### 3️⃣ weak_ptr如何使用？（⭐⭐⭐⭐）

```cpp
shared_ptr<int> sp = make_shared<int>(10);
weak_ptr<int> wp = sp;  // 创建weak_ptr

// ❌ 不能直接访问
// *wp;  // 编译错误

// ✅ 必须先转换为shared_ptr
if (auto sp2 = wp.lock()) {  // 🔑 lock()返回shared_ptr
    cout << *sp2;  // 安全访问
} else {
    cout << "对象已释放";
}

// 检查对象是否还存在
if (wp.expired()) {  // 等价于 use_count() == 0
    cout << "对象已释放";
}
```

#### 4️⃣ 多线程下shared_ptr安全吗？（⭐⭐⭐⭐）

**部分安全：**
```cpp
// ✅ 引用计数的增减是线程安全的（原子操作）
shared_ptr<int> p1 = make_shared<int>(10);
thread t1([p1]{ auto p2 = p1; });  // 安全
thread t2([p1]{ auto p3 = p1; });  // 安全

// ❌ 对象的读写不是线程安全的
shared_ptr<int> p = make_shared<int>(0);
thread t1([&p]{ (*p)++; });  // ❌ 数据竞争
thread t2([&p]{ (*p)++; });  // ❌ 数据竞争
// 需要额外加锁

// ❌ 同一个shared_ptr被多个线程修改不安全
shared_ptr<int> p;
thread t1([&p]{ p = make_shared<int>(1); });  // ❌ 竞争
thread t2([&p]{ p = make_shared<int>(2); });  // ❌ 竞争
```

🔑 **得分点：能区分引用计数安全 vs 对象数据安全**

### 🎓 面试回答模板

```
【标准回答】
C++11提供三种智能指针，都基于RAII思想：

1. unique_ptr：独占所有权，不可复制，只能移动
   适用于明确唯一所有权的场景

2. shared_ptr：共享所有权，使用引用计数
   多个指针可以共享同一个对象
   引用计数为0时自动释放

3. weak_ptr：不拥有对象，配合shared_ptr使用
   解决shared_ptr的循环引用问题

【追问-为什么用make_shared】
make_shared一次分配对象和控制块，而new需要两次分配，
性能更好且异常安全。

【追问-循环引用】
双向链表、父子节点互相持有时，会出现循环引用导致内存泄漏。
解决方法是一方用shared_ptr，另一方用weak_ptr。

【追问-线程安全】
引用计数的增减是原子操作，线程安全。
但对象本身的读写不安全，需要额外同步。
```

### ⚠️ 常见误区

❌ **错误1**：以为智能指针完全不会内存泄漏
✅ **正确**：循环引用仍会泄漏，需要weak_ptr

❌ **错误2**：以为shared_ptr线程完全安全
✅ **正确**：只有引用计数安全，对象数据不安全

❌ **错误3**：unique_ptr不能作为函数返回值
✅ **正确**：可以返回，会触发移动语义

### 🌟 加分点

- 提到shared_ptr的控制块包含自定义删除器
- 知道enable_shared_from_this的用法
- 了解make_unique（C++14）和make_shared的区别
- 提到unique_ptr零开销（相比原始指针）

---

## 📌 new和malloc的区别（⭐⭐⭐⭐⭐ 必考）

### 🎯 得分关键词
> **构造函数** | **析构函数** | **operator new** | **类型安全** | **异常处理** | **重载** | **自由存储区vs堆**

### ✅ 核心区别对比

| 特性 | new/delete | malloc/free |
|------|-----------|-------------|
| **本质** | C++关键字 | C标准库函数 |
| **类型** | 类型安全，自动计算大小 | 需手动指定字节数 |
| **构造/析构** | ✅ 调用 | ❌ 不调用 |
| **失败处理** | 抛出std::bad_alloc异常 | 返回NULL |
| **内存来源** | 自由存储区(free store) | 堆(heap) |
| **可重载** | ✅ 可重载operator new | ❌ 不可重载 |
| **返回类型** | 对象类型指针 | void*（需强转） |

### 💻 代码示例

```cpp
// ============ malloc/free ============
// 1️⃣ 需要手动计算大小
int* p1 = (int*)malloc(sizeof(int));  // 需要强制转换
*p1 = 10;
free(p1);

// 2️⃣ 不会调用构造/析构
class Test {
public:
    Test() { cout << "构造"; }
    ~Test() { cout << "析构"; }
};

Test* p2 = (Test*)malloc(sizeof(Test));
free(p2);  // ❌ 不会输出"构造"和"析构"

// ============ new/delete ============
// 1️⃣ 自动计算大小，类型安全
int* p3 = new int(10);  // 无需sizeof，无需强转
delete p3;

// 2️⃣ 调用构造/析构
Test* p4 = new Test();  // ✅ 输出"构造"
delete p4;              // ✅ 输出"析构"

// 3️⃣ 数组形式
int* arr1 = new int[10];
delete[] arr1;  // 🔑 注意：数组必须用delete[]

Test* arr2 = new Test[3];  // 调用3次构造函数
delete[] arr2;             // 调用3次析构函数
```

### 💡 底层实现（⭐⭐⭐⭐⭐ 必问）

**new的执行过程：**
```cpp
Test* p = new Test(10);

// 等价于：
// 1️⃣ 调用operator new分配内存
void* mem = operator new(sizeof(Test));

// 2️⃣ 在分配的内存上调用构造函数（placement new）
Test* p = new(mem) Test(10);

// 3️⃣ 返回类型化指针
return p;
```

**delete的执行过程：**
```cpp
delete p;

// 等价于：
// 1️⃣ 调用析构函数
p->~Test();

// 2️⃣ 调用operator delete释放内存
operator delete(p);
```

**malloc底层：**
```cpp
void* malloc(size_t size) {
    // 实际分配 size + 4 字节
    // 前4字节存储分配的大小

    [4字节：大小信息][实际数据...]
                     ↑
                  返回这个地址
}

void free(void* ptr) {
    // 向前偏移4字节，读取大小信息
    size_t size = *((size_t*)ptr - 1);
    // 释放整块内存
}
```

### 🔥 面试追问点

#### 1️⃣ 为什么数组delete要用delete[]？（⭐⭐⭐⭐⭐ 必问）

```cpp
class Test {
public:
    ~Test() { delete[] data; }
    int* data = new int[100];
};

Test* arr = new Test[3];  // 创建3个对象

// ❌ 错误：只调用一次析构
delete arr;
// 只有arr[0]的析构被调用
// arr[1]和arr[2]的data内存泄漏！

// ✅ 正确：调用3次析构
delete[] arr;
// arr[0]、arr[1]、arr[2]都正确析构
```

**原理：**
```
new[]分配的内存布局：
[4字节：数组大小] [对象1] [对象2] [对象3]
                  ↑
               返回地址

delete[]: 读取前4字节，知道有3个对象，调用3次析构
delete:   不知道数组大小，只调用1次析构
```

#### 2️⃣ 可以重载new和delete吗？（⭐⭐⭐⭐）

✅ **可以！有两种重载方式**

**方式1：全局重载**
```cpp
// 重载全局operator new
void* operator new(size_t size) {
    cout << "自定义new，分配 " << size << " 字节\n";
    void* p = malloc(size);
    if (!p) throw std::bad_alloc();
    return p;
}

void operator delete(void* p) noexcept {
    cout << "自定义delete\n";
    free(p);
}

// 使用
int* p = new int;  // 调用自定义operator new
delete p;          // 调用自定义operator delete
```

**方式2：类内重载**
```cpp
class Test {
public:
    // 🔑 必须是static（即使不写也是static）
    static void* operator new(size_t size) {
        cout << "Test的operator new\n";
        return malloc(size);
    }

    static void operator delete(void* p) {
        cout << "Test的operator delete\n";
        free(p);
    }
};
```

**应用场景：**
- 内存池：自定义内存分配策略
- 调试：追踪内存分配
- 对齐：特定的内存对齐需求

#### 3️⃣ placement new是什么？（⭐⭐⭐⭐）

**在已分配的内存上构造对象：**
```cpp
// 1️⃣ 先分配内存
char buffer[sizeof(Test)];

// 2️⃣ 在指定内存上构造对象
Test* p = new(buffer) Test(10);  // placement new
// 🔑 不分配内存，只调用构造函数

// 3️⃣ 使用
p->func();

// 4️⃣ 手动析构（不能用delete，因为内存不是动态分配的）
p->~Test();
```

**应用场景：**
- 内存池管理
- 共享内存中构造对象
- 避免重复分配释放

#### 4️⃣ free如何知道要释放多少内存？（⭐⭐⭐⭐）

```cpp
// malloc实际分配的内存布局
┌──────────┬─────────────────────┐
│ 4字节：size │   用户数据 (size字节)  │
└──────────┴─────────────────────┘
            ↑
         返回地址

// free实现伪代码
void free(void* ptr) {
    if (!ptr) return;

    // 向前4字节读取大小
    size_t* p = (size_t*)ptr - 1;
    size_t size = *p;

    // 释放整块（包括前4字节）
    real_free(p, size + 4);
}
```

### 🎓 面试回答模板

```
【标准回答】
new和malloc主要有以下区别：

1. 本质不同
   - new是C++关键字，malloc是C标准库函数

2. 构造和析构
   - new会调用构造函数和析构函数
   - malloc只分配内存，不调用构造/析构

3. 类型安全
   - new自动计算大小，返回正确类型指针
   - malloc需要手动计算大小，返回void*需要强转

4. 失败处理
   - new失败抛出bad_alloc异常
   - malloc失败返回NULL

5. 底层实现
   - new底层调用operator new（可重载），再调用构造函数
   - malloc直接向系统申请内存

【追问-为什么数组要delete[]】
因为new[]会在内存前4字节记录数组大小，
delete[]根据这个信息调用多次析构，
普通delete只调用一次析构，导致其他对象泄漏。

【追问-可以混用吗】
不可以。new和delete配对，malloc和free配对。
混用会导致未定义行为。
```

### ⚠️ 常见误区

❌ **错误1**：以为new和malloc可以混用
✅ **正确**：必须配对使用，new↔delete，malloc↔free

❌ **错误2**：new失败返回NULL
✅ **正确**：new失败抛异常，new(nothrow)才返回NULL

❌ **错误3**：delete[]和delete可以互换
✅ **正确**：数组必须用delete[]，否则只析构第一个元素

### 🌟 加分点

- 提到new_handler（内存不足时的回调）
- 知道nothrow版本：`new(nothrow) int`
- 了解aligned_alloc（C++17对齐分配）
- 提到自由存储区和堆的区别（概念上）

---

## 📌 const关键字（⭐⭐⭐⭐⭐ 必考）

### 🎯 得分关键词
> **const指针** | **指向const的指针** | **const成员函数** | **编译时检查** | **顶层const** | **底层const**

### ✅ const的五种用法

#### 1️⃣ 修饰变量
```cpp
const int a = 10;
a = 20;  // ❌ 编译错误：不能修改const变量
```

#### 2️⃣ 修饰指针（重点！）
```cpp
// 🔑 看const在*的哪一边

// ① 指向常量的指针（pointer to const）
const int* p1 = &a;
int const* p1 = &a;  // 等价
// 不能修改*p1，但可以修改p1本身
*p1 = 20;  // ❌ 错误
p1 = &b;   // ✅ 正确

// ② 常量指针（const pointer）
int* const p2 = &a;
// 不能修改p2，但可以修改*p2
*p2 = 20;  // ✅ 正确
p2 = &b;   // ❌ 错误

// ③ 指向常量的常量指针
const int* const p3 = &a;
// p3和*p3都不能修改
*p3 = 20;  // ❌ 错误
p3 = &b;   // ❌ 错误

// 🎯 记忆技巧：
// const在*左边 → 不能改内容（*p）
// const在*右边 → 不能改指针（p）
```

#### 3️⃣ 修饰引用
```cpp
const int& ref = a;
ref = 20;  // ❌ 错误：不能修改const引用

// 🌟 const引用可以绑定到临时对象
const int& r1 = 10;  // ✅ 正确
int& r2 = 10;        // ❌ 错误
```

#### 4️⃣ 修饰成员函数（⭐⭐⭐⭐⭐ 高频）
```cpp
class Test {
    int value;
public:
    // const成员函数：不能修改成员变量
    int getValue() const {
        return value;  // ✅ 只读
        // value = 10;  // ❌ 编译错误
    }

    void setValue(int v) const {
        value = v;  // ❌ 编译错误
    }
};

const Test t;
t.getValue();   // ✅ const对象只能调用const成员函数
t.setValue(10); // ❌ 错误
```

#### 5️⃣ 修饰返回值
```cpp
// 返回const引用（避免拷贝）
const string& getName() const {
    return name;
}

// 返回const指针
const int* getData() const {
    return data;
}
```

### 💡 const成员函数深入理解（⭐⭐⭐⭐⭐ 必问）

**this指针的const性**
```cpp
class Test {
    int value;
public:
    // 非const成员函数
    void func() {
        // this类型: Test* const
        // 指针本身是const，指向的内容可修改
        this->value = 10;  // ✅
    }

    // const成员函数
    void func() const {
        // this类型: const Test* const
        // 指针和内容都是const
        this->value = 10;  // ❌ 错误
    }
};
```

**mutable关键字**
```cpp
class Test {
    mutable int cache;  // 🔑 mutable修饰
    int value;
public:
    void updateCache() const {
        cache = 100;  // ✅ const函数中可以修改mutable成员
        value = 10;   // ❌ 不能修改普通成员
    }
};

// 应用：缓存、计数器等辅助数据
```

### 🔥 面试追问点

#### 1️⃣ const对象调用哪个函数？（⭐⭐⭐⭐⭐ 必问）

```cpp
class Test {
public:
    void func() {
        cout << "非const版本";
    }

    void func() const {
        cout << "const版本";
    }
};

Test t1;
t1.func();  // 调用：非const版本

const Test t2;
t2.func();  // 调用：const版本

// 🔑 规则：
// - const对象：只能调用const函数
// - 非const对象：优先调用非const函数，没有则调用const函数
```

#### 2️⃣ const_cast是什么？（⭐⭐⭐⭐）

```cpp
// const_cast: 移除const属性（危险！慎用）

const int a = 10;
const int* p = &a;

// 移除const
int* p2 = const_cast<int*>(p);
*p2 = 20;  // ⚠️ 未定义行为！

// ✅ 安全的使用场景：
void func(const char* str) {
    // 如果确定str指向非const内存
    char* p = const_cast<char*>(str);
    p[0] = 'A';  // 修改
}

char buffer[] = "hello";
func(buffer);  // ✅ 安全，buffer本身非const
```

#### 3️⃣ const和#define的区别？（⭐⭐⭐⭐）

| 特性 | const | #define |
|------|-------|---------|
| **类型检查** | ✅ 有类型 | ❌ 文本替换 |
| **作用域** | ✅ 遵守作用域 | ❌ 全局替换 |
| **调试** | ✅ 有符号表 | ❌ 难调试 |
| **内存** | ✅ 有内存地址 | ❌ 无地址 |
| **时机** | 编译时 | 预处理时 |

```cpp
// const: 类型安全
const int MAX = 100;
int arr[MAX];  // ✅ 正确

// #define: 容易出错
#define MAX 100
#define MIN MAX - 1  // 危险
int x = 10 * MIN;  // 实际是: 10 * 100 - 1 = 999（而不是90）

// 解决：必须加括号
#define MIN (MAX - 1)
```

### 🎓 面试回答模板

```
【标准回答】
const主要有以下用法：

1. 修饰变量：定义常量，编译时检查
2. 修饰指针：
   - const int* p: 不能改*p（指向的内容）
   - int* const p: 不能改p（指针本身）
3. 修饰成员函数：不能修改成员变量，const对象只能调用const函数
4. 修饰引用：常引用，可以绑定临时对象

【追问-const成员函数】
const成员函数中，this指针类型是const Test* const，
不能修改成员变量。但mutable修饰的成员除外。

【追问-const和#define区别】
const有类型检查，遵守作用域，有符号表便于调试。
#define是预处理阶段的文本替换，无类型检查，容易出错。
```

### ⚠️ 常见误区

❌ **错误1**：以为const变量完全不可改
✅ **正确**：可以通过const_cast或指针修改（未定义行为）

❌ **错误2**：const成员函数不能修改任何东西
✅ **正确**：可以修改mutable成员，可以修改全局变量

❌ **错误3**：混淆顶层const和底层const
✅ **正确**：顶层const修饰指针本身，底层const修饰指向的对象

---

## STL容器

## 📌 vector底层实现（⭐⭐⭐⭐⭐ 必考）

### 🎯 得分关键词
> **动态数组** | **连续内存** | **capacity** | **size** | **扩容机制** | **成倍增长** | **迭代器失效** | **均摊O(1)**

### ✅ 核心结构

```cpp
template<typename T>
class vector {
private:
    T* _start;      // 数组起始位置
    T* _finish;     // 当前使用的末尾
    T* _end;        // 分配的内存末尾

public:
    size_t size() const { return _finish - _start; }
    size_t capacity() const { return _end - _start; }
};

// 内存布局：
┌────────┬────────┬────────┬────────┬────────┐
│  elem1 │  elem2 │  elem3 │  (空)  │  (空)  │
└────────┴────────┴────────┴────────┴────────┘
↑                          ↑                 ↑
_start                  _finish            _end
|<------- size -------->|
|<----------- capacity ------------->|
```

### 💡 扩容机制（⭐⭐⭐⭐⭐ 高频追问）

**为什么成倍扩容？**

```cpp
// 假设扩容因子为m，需要插入n个元素

// 第1次扩容：复制 m^0 = 1 个元素
// 第2次扩容：复制 m^1 = m 个元素
// 第3次扩容：复制 m^2 个元素
// ...
// 第k次扩容：复制 m^(k-1) 个元素

// 总复制次数 = 1 + m + m^2 + ... + m^(k-1)
//           = (m^k - 1) / (m - 1)  （等比数列求和）
//           ≈ n (因为 m^k ≈ n)

// 平均每次插入复制次数 = n / n = O(1) ✅ 均摊常数时间！
```

**如果固定增长呢？**
```cpp
// 假设每次+100，插入10000个元素

// 扩容100次，每次复制100、200、300...10000个元素
// 总复制次数 = 100 + 200 + ... + 10000
//           = 100 * (1 + 2 + ... + 100)
//           = 100 * 5050 = 505000

// 平均每次插入 = 505000 / 10000 = 50.5次复制 ❌ O(n)!
```

🔑 **结论：成倍增长 → 均摊O(1)，固定增长 → 均摊O(n)**

### 💻 代码实现

**push_back实现：**
```cpp
void push_back(const T& value) {
    if (_finish == _end) {  // 🔑 容量不足，需要扩容
        // 计算新容量（通常是2倍）
        size_t oldCap = capacity();
        size_t newCap = (oldCap == 0) ? 1 : oldCap * 2;

        // 分配新内存
        T* newStart = new T[newCap];

        // 拷贝旧数据
        for (size_t i = 0; i < size(); ++i) {
            newStart[i] = _start[i];
        }

        // 释放旧内存
        delete[] _start;

        // 更新指针
        _start = newStart;
        _finish = _start + oldSize;
        _end = _start + newCap;
    }

    // 在末尾构造新元素
    *_finish = value;
    ++_finish;
}
```

### 🔥 面试追问点

#### 1️⃣ 迭代器失效的情况？（⭐⭐⭐⭐⭐ 必问）

```cpp
vector<int> v = {1, 2, 3, 4, 5};

// ❌ 情况1：插入导致扩容
auto it = v.begin();
v.push_back(6);  // 可能扩容，重新分配内存
*it;  // ❌ it失效！指向旧内存

// ❌ 情况2：删除元素
auto it2 = v.begin() + 2;  // 指向3
v.erase(it2);  // 删除3
*it2;  // ❌ it2失效！

// ✅ 正确做法：使用erase的返回值
for (auto it = v.begin(); it != v.end(); ) {
    if (*it == 3) {
        it = v.erase(it);  // erase返回下一个有效迭代器
    } else {
        ++it;
    }
}

// ❌ 情况3：swap
vector<int> v2;
auto it3 = v.begin();
v.swap(v2);
*it3;  // ❌ it3失效
```

**总结：**
- 插入：可能全部失效（扩容时）
- 删除：删除点之后全部失效
- swap：全部失效

#### 2️⃣ reserve和resize的区别？（⭐⭐⭐⭐）

```cpp
vector<int> v;

// reserve: 只改capacity，不改size
v.reserve(100);
cout << v.size();      // 0
cout << v.capacity();  // 100
v[0] = 10;  // ❌ 越界！size还是0

// resize: 同时改size和capacity
v.resize(100);
cout << v.size();      // 100
cout << v.capacity();  // ≥100
v[0] = 10;  // ✅ 正确

// resize还会初始化元素
v.resize(100, 42);  // 所有元素初始化为42
```

**使用场景：**
```cpp
// 预知大小，避免多次扩容
vector<int> v;
v.reserve(10000);  // 🌟 一次性分配，避免多次扩容
for (int i = 0; i < 10000; ++i) {
    v.push_back(i);  // 不会扩容
}
```

#### 3️⃣ vector<bool>为什么特殊？（⭐⭐⭐）

```cpp
vector<bool> v = {true, false, true};

// ❌ vector<bool>不是真正的vector
bool* p = &v[0];  // 编译错误！

// 原因：vector<bool>的特化版本
// 内部用bit位存储，每个bool只占1bit，而不是1byte
// 8个bool共享1个byte

// 问题：
auto x = v[0];  // x不是bool&，是代理类
x = false;      // 不会修改v[0]！

// 解决：不要用vector<bool>，用deque<bool>或bitset
```

#### 4️⃣ 如何释放vector的内存？（⭐⭐⭐⭐）

```cpp
vector<int> v(10000);
v.clear();  // ❌ 只清空元素，不释放内存
cout << v.capacity();  // 仍然是10000

// ✅ 方法1：swap技巧
vector<int>().swap(v);  // 用空vector交换
cout << v.capacity();  // 0

// ✅ 方法2：shrink_to_fit (C++11)
v.shrink_to_fit();  // 请求释放多余内存（不保证）

// ✅ 方法3：移动到局部作用域
{
    vector<int> temp(std::move(v));
}  // temp离开作用域，释放内存
```

### 🎓 面试回答模板

```
【标准回答】
vector是动态数组，底层是一块连续内存。

核心成员：
- _start: 数组起始位置
- _finish: 当前使用的末尾
- _end: 分配的内存末尾

扩容机制：
- 当size==capacity时，分配新内存（通常2倍）
- 拷贝所有元素到新内存
- 释放旧内存
- 均摊时间复杂度O(1)

【追问-为什么成倍扩容】
成倍扩容保证均摊O(1)插入。
如果固定增长（如每次+100），均摊复杂度是O(n)。

【追问-迭代器失效】
三种情况：
1. 插入可能扩容，全部失效
2. 删除，删除点之后失效
3. swap，全部失效

解决：使用erase返回值，或重新获取迭代器。

【追问-如何减少扩容】
预先reserve足够大小，避免多次扩容和拷贝。
```

### ⚠️ 常见误区

❌ **错误1**：clear()会释放内存
✅ **正确**：clear()只清空元素，capacity不变

❌ **错误2**：reserve(n)后可以直接用下标访问
✅ **正确**：reserve不改size，要用push_back或resize

❌ **错误3**：迭代器永远有效
✅ **正确**：插入、删除、扩容都可能导致失效

### 🌟 加分点

- 提到emplace_back比push_back性能更好（减少拷贝）
- 知道vector的内存不会自动收缩
- 了解vector的异常安全性（强异常保证）
- 提到小对象优化(SSO)在某些实现中的应用

---

## 📌 红黑树（⭐⭐⭐⭐ 高频）

### 🎯 得分关键词
> **平衡二叉搜索树** | **5条性质** | **O(logn)** | **变色** | **旋转** | **map/set底层** | **黑高**

### ✅ 红黑树5条性质

```cpp
1️⃣ 节点是红色或黑色
2️⃣ 根节点是黑色
3️⃣ 叶子节点(NULL)是黑色
4️⃣ 红色节点的子节点必须是黑色（不能有连续的红节点）
5️⃣ 从任一节点到叶子的所有路径包含相同数量的黑节点（黑高）

🔑 核心：保证最长路径 ≤ 2 × 最短路径
```

**示例：**
```
           (10黑)
          /      \
      (5红)      (15红)
      /  \        /   \
  (3黑)(7黑) (12黑)(18黑)

路径1: 10→5→3→NULL，黑节点数=3
路径2: 10→15→12→NULL，黑节点数=3
路径3: 10→5→7→NULL，黑节点数=3
✅ 所有路径黑节点数相同
```

### 💡 为什么用红黑树？（⭐⭐⭐⭐⭐ 高频追问）

**红黑树 vs AVL树：**

| 特性 | 红黑树 | AVL树 |
|------|-------|-------|
| **平衡性** | 近似平衡 | 严格平衡 |
| **高度** | ≤ 2log(n+1) | ≤ 1.44log(n+2) |
| **插入** | 最多3次旋转 | 可能多次旋转 |
| **删除** | 最多3次旋转 | 可能多次旋转 |
| **查找** | 稍慢 | 更快 |
| **适用** | 插入删除多 | 查询多 |

```cpp
// AVL: 严格平衡，左右子树高度差≤1
      10
     /  \
    5    15    // 高度差 = |1-1| = 0 ✅
   /
  3

// 红黑树: 近似平衡，最长≤2×最短
      10(黑)
     /      \
   5(红)   15(黑)  // 可以有高度差，但总体平衡
  /  \
3(黑) 7(黑)
```

🔑 **结论：**
- **查询为主**：AVL树更优（更平衡）
- **插删为主**：红黑树更优（旋转少）
- **STL的选择**：map/set用红黑树（插删查综合考虑）

### 🔥 面试追问点

#### 1️⃣ 插入节点什么颜色？为什么？（⭐⭐⭐⭐⭐）

🔴 **新插入节点一定是红色！**

**原因：**
```
如果插入黑色：
❌ 破坏性质5（所有路径黑节点数相同）
   修复成本高，需要调整整棵树

如果插入红色：
✅ 只可能破坏性质4（红色节点子节点是黑色）
   修复成本低，最多3次旋转
```

**插入后的修复：**
```cpp
// Case 1: 叔叔节点是红色
// 解决：父亲和叔叔变黑，爷爷变红，继续向上调整
      G(黑)                  G(红)
     /    \      →         /    \
  P(红)   U(红)         P(黑)   U(黑)
   /                     /
 N(红)                 N(红)

// Case 2: 叔叔是黑色，N是右孩子
// 解决：左旋，转换为Case3
    G(黑)              G(黑)
   /    \             /    \
 P(红)  U(黑)  →   N(红)  U(黑)
    \               /
   N(红)         P(红)

// Case 3: 叔叔是黑色，N是左孩子
// 解决：右旋+变色
    G(黑)              P(黑)
   /    \      →      /    \
 P(红)  U(黑)       N(红)  G(红)
 /                           \
N(红)                        U(黑)
```

#### 2️⃣ 最多旋转几次？（⭐⭐⭐⭐）

- **插入**：最多2次旋转
- **删除**：最多3次旋转

🔑 **得分点：能说出具体次数**

#### 3️⃣ 为什么不用B树？（⭐⭐⭐）

```
B树：多叉平衡树，节点可以有多个key
适用场景：磁盘存储，减少IO次数

红黑树：二叉树，所有数据在内存
优势：
1. 内存中，二叉树局部性更好（缓存友好）
2. 实现简单
3. 常数因子小

🔑 数据库索引用B+树（磁盘），STL用红黑树（内存）
```

### 🎓 面试回答模板

```
【标准回答】
红黑树是一种平衡二叉搜索树，保证5条性质：
1. 节点红或黑
2. 根是黑色
3. 叶子(NULL)是黑色
4. 红色节点的子节点是黑色
5. 任意节点到叶子的路径，黑节点数相同

时间复杂度：查找、插入、删除都是O(logn)

【追问-为什么用红黑树不用AVL】
红黑树是近似平衡，插入删除最多旋转3次。
AVL是严格平衡，可能多次旋转。
STL选择红黑树，因为插删查综合性能更好。

【追问-新节点什么颜色】
红色。如果插入黑色会破坏所有路径黑节点数相同的性质，
修复代价大。插入红色只可能破坏不能有连续红节点的性质，
通过变色和旋转（最多2次）就能修复。
```

### ⚠️ 常见误区

❌ **错误1**：以为红黑树完全平衡
✅ **正确**：近似平衡，最长≤2×最短

❌ **错误2**：以为插入删除总是要旋转
✅ **正确**：很多情况只需变色，不需要旋转

❌ **错误3**：混淆红黑树和AVL树
✅ **正确**：红黑树牺牲平衡性换取插删性能

---

## 📌 多继承与菱形继承（⭐⭐⭐⭐⭐ 必考）

### 🎯 得分关键词
> **多继承** | **菱形继承** | **虚继承** | **virtual** | **二义性** | **虚基类** | **构造顺序** | **内存布局**

### ✅ 多继承基础

**什么是多继承：**
```cpp
class A {
public:
    void funcA() { cout << "A"; }
};

class B {
public:
    void funcB() { cout << "B"; }
};

// C多继承自A和B
class C : public A, public B {
public:
    void funcC() {
        funcA();  // ✅ 继承A的方法
        funcB();  // ✅ 继承B的方法
    }
};

C c;
c.funcA();  // ✅ 正确
c.funcB();  // ✅ 正确
```

### 💡 菱形继承问题（⭐⭐⭐⭐⭐ 高频追问）

**什么是菱形继承：**
```
       Animal
       /    \
      /      \
   Tiger    Lion
      \      /
       \    /
       Liger
```

**代码示例：**
```cpp
class Animal {
public:
    int age;
    void eat() { cout << "Animal eat"; }
};

class Tiger : public Animal {
public:
    void roar() { cout << "Tiger roar"; }
};

class Lion : public Animal {
public:
    void hunt() { cout << "Lion hunt"; }
};

// ❌ 菱形继承问题
class Liger : public Tiger, public Lion {
public:
    void show() {
        // ❌ 二义性！有两个age：Tiger::age 和 Lion::age
        // age = 10;  // 编译错误：ambiguous

        // 必须明确指定
        Tiger::age = 10;
        Lion::age = 5;

        // ❌ 浪费内存：Animal的成员被复制了两份
    }
};
```

**内存布局：**
```
Liger对象内存：
┌─────────────────────┐
│  Tiger部分          │
│  ├─ Animal::age     │  ← 第一份Animal数据
│  └─ Tiger数据       │
├─────────────────────┤
│  Lion部分           │
│  ├─ Animal::age     │  ← 第二份Animal数据（冗余！）
│  └─ Lion数据        │
├─────────────────────┤
│  Liger数据          │
└─────────────────────┘
```

### 💻 虚继承解决方案（⭐⭐⭐⭐⭐ 必问）

**使用虚继承：**
```cpp
class Animal {
public:
    int age;
    Animal() { cout << "Animal构造\n"; }
};

// 🔑 关键：使用virtual继承
class Tiger : virtual public Animal {
public:
    Tiger() { cout << "Tiger构造\n"; }
};

class Lion : virtual public Animal {
public:
    Lion() { cout << "Lion构造\n"; }
};

class Liger : public Tiger, public Lion {
public:
    Liger() { cout << "Liger构造\n"; }

    void show() {
        age = 10;  // ✅ 正确！只有一个age
        eat();     // ✅ 正确！只有一个eat()
    }
};
```

**虚继承后的内存布局：**
```
Liger对象内存：
┌─────────────────────┐
│  Tiger部分          │
│  └─ vbptr ────┐     │  ← 虚基类指针
├───────────────┼─────┤
│  Lion部分     │     │
│  └─ vbptr ────┤     │  ← 虚基类指针
├───────────────┤     │
│  Liger数据    │     │
├───────────────┤     │
│  Animal部分   │◄────┘  ← 只有一份Animal数据
│  └─ age       │
└─────────────────────┘
```

### 🔥 面试追问点

#### 1️⃣ 虚继承的构造顺序？（⭐⭐⭐⭐⭐ 必问）

```cpp
class Animal {
public:
    Animal() { cout << "1. Animal\n"; }
};

class Tiger : virtual public Animal {
public:
    Tiger() { cout << "2. Tiger\n"; }
};

class Lion : virtual public Animal {
public:
    Lion() { cout << "3. Lion\n"; }
};

class Liger : public Tiger, public Lion {
public:
    Liger() { cout << "4. Liger\n"; }
};

Liger l;
// 输出顺序：
// 1. Animal    ← 🔑 虚基类最先构造
// 2. Tiger
// 3. Lion
// 4. Liger
```

🔑 **得分点：虚基类由最终派生类构造，且最先构造**

#### 2️⃣ 虚继承的性能开销？（⭐⭐⭐⭐）

```cpp
// 虚继承的代价：
1. 额外的虚基类指针（vbptr）
2. 间接访问成员（通过vbptr查表）
3. 构造函数复杂度增加

// 空间开销：
sizeof(Tiger)  // 普通继承：Animal大小 + Tiger大小
sizeof(Tiger)  // 虚继承：Animal大小 + Tiger大小 + vbptr(8字节)
```

#### 3️⃣ 什么时候使用多继承？（⭐⭐⭐⭐）

```cpp
// ✅ 合理使用场景：接口继承
class Serializable {
public:
    virtual void serialize() = 0;
};

class Comparable {
public:
    virtual int compare(const Comparable&) = 0;
};

class Student : public Serializable, public Comparable {
    // ✅ 继承多个接口（纯虚类），没有菱形继承问题
};

// ❌ 不推荐：继承多个有状态的类
class Car : public Engine, public Wheel {
    // ❌ 容易出现菱形继承和二义性问题
};
```

🔑 **得分点：多继承主要用于接口继承，避免继承多个有状态的类**

### 🎓 面试回答模板

```
【标准回答】
多继承允许一个类继承多个基类，但会引发菱形继承问题：

菱形继承：
      A
     / \
    B   C
     \ /
      D

问题：
1. 二义性：D中有两份A的成员，访问时编译器不知道用哪个
2. 内存浪费：A的数据被存储两份

解决方案：虚继承（virtual）
- B和C虚继承A，D中只保留一份A的数据
- 通过虚基类指针（vbptr）访问虚基类成员
- 虚基类由最终派生类构造，且最先构造

【追问-虚继承构造顺序】
虚基类最先构造，由最终派生类的构造函数负责调用。
即使中间类（B、C）的初始化列表中调用了A的构造，
也会被忽略，由D的构造函数统一调用A的构造。

【追问-性能开销】
虚继承有额外开销：
1. 每个虚继承类增加一个vbptr（8字节）
2. 访问虚基类成员需要间接寻址
3. 构造和析构逻辑更复杂

【追问-使用建议】
多继承主要用于接口继承（纯虚类），
避免继承多个有状态的类，优先考虑组合而非继承。
```

### ⚠️ 常见误区

❌ **错误1**：以为虚继承没有性能开销
✅ **正确**：有vbptr和间接访问的开销

❌ **错误2**：虚继承时基类构造顺序与普通继承相同
✅ **正确**：虚基类最先构造，由最终派生类负责

❌ **错误3**：多继承总是不好的
✅ **正确**：接口继承（多个纯虚类）是合理的

### 🌟 加分点

- 提到vbptr（虚基类指针）和vtable的区别
- 知道C++标准库避免多继承（除了iostream体系）
- 了解其他语言对多继承的处理（Java禁止，Python允许）
- 提到组合优于继承原则

---

## 📌 static关键字（⭐⭐⭐⭐⭐ 必考）

### 🎯 得分关键词
> **静态成员变量** | **静态成员函数** | **静态局部变量** | **类外初始化** | **生命周期** | **内部链接** | **单例模式**

### ✅ static的五种用法

#### 1️⃣ 修饰局部变量（⭐⭐⭐⭐⭐）

```cpp
void func() {
    static int count = 0;  // 🔑 只初始化一次
    count++;
    cout << count << endl;
}

func();  // 输出：1
func();  // 输出：2
func();  // 输出：3

// 🔑 特点：
// 1. 生命周期：程序开始到结束（不是函数调用期间）
// 2. 作用域：仅在函数内可见
// 3. 初始化：第一次执行到该语句时初始化
// 4. 存储位置：静态存储区（不是栈）
```

#### 2️⃣ 修饰全局变量/函数（⭐⭐⭐⭐）

```cpp
// file1.cpp
static int g_value = 10;  // 🔑 仅在file1.cpp可见
static void helper() {    // 🔑 仅在file1.cpp可见
    cout << g_value;
}

// file2.cpp
extern int g_value;  // ❌ 链接错误：找不到g_value
// 因为g_value是静态的，只在file1.cpp可见

// 🔑 作用：限制链接性（internal linkage）
// 类似于匿名命名空间
namespace {
    int g_value = 10;  // 等价于static int g_value
}
```

#### 3️⃣ 修饰类的成员变量（⭐⭐⭐⭐⭐ 高频）

```cpp
class Test {
public:
    static int count;  // 🔑 声明（不是定义）
    int value;

    Test() {
        count++;  // 所有对象共享
        value = 0;
    }
};

// 🔑 必须在类外定义和初始化
int Test::count = 0;

Test t1;  // count = 1
Test t2;  // count = 2
cout << Test::count;  // 输出：2

// 🔑 特点：
// 1. 所有对象共享一个副本
// 2. 不属于任何对象，属于类
// 3. 可以通过类名访问：Test::count
// 4. 必须在类外初始化（除了const static整型）
```

**内存布局：**
```
静态存储区：
┌──────────────┐
│ Test::count  │  ← 所有Test对象共享
└──────────────┘

对象t1：              对象t2：
┌──────────┐         ┌──────────┐
│  value   │         │  value   │
└──────────┘         └──────────┘
```

#### 4️⃣ 修饰类的成员函数（⭐⭐⭐⭐⭐ 高频）

```cpp
class Test {
    static int count;
    int value;
public:
    // 静态成员函数
    static void setCount(int c) {
        count = c;   // ✅ 可以访问静态成员
        // value = 10;  // ❌ 错误！不能访问非静态成员
        // this->value; // ❌ 错误！没有this指针
    }

    static int getCount() {
        return count;
    }
};

// 调用方式1：通过类名
Test::setCount(100);

// 调用方式2：通过对象
Test t;
t.setCount(100);  // 不推荐，容易误导

// 🔑 特点：
// 1. 没有this指针
// 2. 只能访问静态成员
// 3. 不能是虚函数
// 4. 不能是const函数
```

#### 5️⃣ const static特殊情况（⭐⭐⭐⭐）

```cpp
class Test {
public:
    // ✅ const static整型可以类内初始化
    const static int MAX = 100;

    // ❌ 非整型必须类外初始化
    const static double PI;  // 声明
};

const double Test::PI = 3.14;  // 类外定义

// C++17: inline static
class Modern {
public:
    inline static int count = 0;  // ✅ C++17可以类内初始化
    inline static string name = "test";  // ✅
};
```

### 🔥 面试追问点

#### 1️⃣ 为什么静态成员变量要类外初始化？（⭐⭐⭐⭐⭐ 必问）

```cpp
class Test {
    static int count = 0;  // ❌ 错误（除了const static整型）
};

// 原因：
1. 类的声明可能出现在多个文件中（通过include）
2. 如果允许类内初始化，每个include的地方都会初始化一次
3. 违反了"只定义一次"的原则

// 正确做法：
// Test.h
class Test {
    static int count;  // 声明
};

// Test.cpp
int Test::count = 0;  // 定义（只在一个cpp文件中）
```

#### 2️⃣ 静态成员函数可以是虚函数吗？（⭐⭐⭐⭐⭐ 高频）

❌ **不可以**

```cpp
class Base {
public:
    static virtual void func();  // ❌ 编译错误
};

// 原因：
1. 虚函数通过对象的vptr调用
2. 静态函数没有this指针，没有对象
3. 静态函数属于类，不属于对象
4. 静态函数在编译时就确定了地址，不需要动态绑定
```

#### 3️⃣ 静态局部变量线程安全吗？（⭐⭐⭐⭐）

```cpp
void func() {
    static int count = 0;  // ❌ 多线程不安全
    count++;
}

// C++11之前：不安全
thread t1(func);
thread t2(func);
// 可能丢失count++

// C++11之后：初始化是线程安全的
void func() {
    static Singleton s;  // ✅ C++11保证只初始化一次
}

// 但读写仍不安全：
void func() {
    static int count = 0;  // 初始化安全
    count++;  // ❌ 读写不安全，需要加锁
}
```

#### 4️⃣ 单例模式的实现？（⭐⭐⭐⭐⭐ 必问）

**饿汉式（线程安全）：**
```cpp
class Singleton {
private:
    static Singleton instance;  // 类加载时初始化
    Singleton() {}
public:
    static Singleton& getInstance() {
        return instance;
    }
};

Singleton Singleton::instance;  // 程序启动时初始化
```

**懒汉式（C++11，线程安全）：**
```cpp
class Singleton {
private:
    Singleton() {}
public:
    static Singleton& getInstance() {
        static Singleton instance;  // 🔑 第一次调用时初始化
        return instance;            // C++11保证线程安全
    }

    // 禁止拷贝
    Singleton(const Singleton&) = delete;
    Singleton& operator=(const Singleton&) = delete;
};
```

### 🎓 面试回答模板

```
【标准回答】
static关键字主要有五种用法：

1. 静态局部变量
   - 生命周期：程序开始到结束
   - 作用域：函数内
   - 只初始化一次

2. 静态全局变量/函数
   - 限制链接性，只在当前文件可见
   - 防止命名冲突

3. 静态成员变量
   - 所有对象共享
   - 属于类，不属于对象
   - 必须类外初始化（C++17前）

4. 静态成员函数
   - 没有this指针
   - 只能访问静态成员
   - 不能是虚函数

【追问-为什么要类外初始化】
因为类声明可能出现在多个文件中，
如果允许类内初始化，会导致重复定义。
只在一个cpp文件中定义，保证单一定义。

【追问-静态函数为什么不能是虚函数】
虚函数需要通过对象的vptr动态绑定，
而静态函数没有this指针，没有对象，
在编译时就确定了地址，不需要动态绑定。

【追问-单例模式】
C++11后推荐使用懒汉式+静态局部变量，
因为C++11保证静态局部变量初始化是线程安全的。
```

### ⚠️ 常见误区

❌ **错误1**：静态成员变量可以在构造函数中初始化
✅ **正确**：必须在类外初始化（除了const static整型）

❌ **错误2**：静态成员函数可以访问非静态成员
✅ **正确**：只能访问静态成员，因为没有this指针

❌ **错误3**：静态局部变量在栈上
✅ **正确**：在静态存储区，生命周期是整个程序

### 🌟 加分点

- 提到C++17的inline static
- 知道静态局部变量初始化的线程安全性（C++11）
- 了解Meyer's Singleton（静态局部变量实现单例）
- 提到static与匿名命名空间的等价性

---

## 📌 右值引用与移动语义（⭐⭐⭐⭐⭐ 必考）

### 🎯 得分关键词
> **左值** | **右值** | **右值引用** | **std::move** | **移动构造** | **移动赋值** | **完美转发** | **std::forward**

### ✅ 左值和右值的区别

```cpp
// 左值（lvalue）：有名字，可以取地址
int a = 10;      // a是左值
int* p = &a;     // ✅ 可以取地址

// 右值（rvalue）：临时对象，不能取地址
int b = 20;
int c = a + b;   // a+b是右值（临时结果）
// int* p = &(a+b);  // ❌ 错误：不能取右值的地址

// 字符串字面量
string s = "hello";  // "hello"是右值

// 函数返回值
int func() { return 42; }
int x = func();  // func()返回值是右值
```

**判断技巧：**
```cpp
能放在 = 左边的是左值
只能放在 = 右边的是右值

int a = 10;  // a是左值
10 = a;      // ❌ 错误：10是右值，不能被赋值
```

### 💡 右值引用（⭐⭐⭐⭐⭐ 核心）

**语法：**
```cpp
// 左值引用（&）
int a = 10;
int& ref1 = a;        // ✅ 左值引用绑定左值
// int& ref2 = 10;    // ❌ 错误：不能绑定右值

const int& ref3 = 10; // ✅ const左值引用可以绑定右值

// 右值引用（&&）
int&& ref4 = 10;      // ✅ 右值引用绑定右值
int&& ref5 = a + 10;  // ✅ 绑定临时对象

// int&& ref6 = a;    // ❌ 错误：不能绑定左值
int&& ref7 = std::move(a);  // ✅ move将左值转为右值
```

### 💻 移动语义（⭐⭐⭐⭐⭐ 必问）

**为什么需要移动语义：**
```cpp
class BigData {
    int* data;
    size_t size;
public:
    // 拷贝构造：深拷贝，慢
    BigData(const BigData& other) {
        size = other.size;
        data = new int[size];
        memcpy(data, other.data, size * sizeof(int));  // 🐌 慢
        cout << "拷贝构造\n";
    }

    // 移动构造：窃取资源，快
    BigData(BigData&& other) noexcept {
        data = other.data;      // 🔑 窃取指针
        size = other.size;
        other.data = nullptr;   // 🔑 清空原对象
        other.size = 0;
        cout << "移动构造\n";
    }
};

BigData func() {
    BigData temp(1000000);
    return temp;  // 🔑 返回时触发移动构造
}

BigData b = func();  // 输出：移动构造（不是拷贝构造）
```

**移动语义的效率对比：**
```
拷贝构造：
  源对象   →  新对象
  [data] ─复制→ [data]
  保留原数据    深拷贝

移动构造：
  源对象   →  新对象
  [data] ─窃取→ [data]
  [NULL]        直接转移指针
  清空资源      无拷贝
```

### 🔥 面试追问点

#### 1️⃣ std::move做了什么？（⭐⭐⭐⭐⭐ 必问）

```cpp
template<typename T>
typename remove_reference<T>::type&& move(T&& t) noexcept {
    return static_cast<typename remove_reference<T>::type&&>(t);
}

// 🔑 核心：move只是类型转换，不移动任何东西
// 它将左值转换为右值引用

vector<int> v1 = {1, 2, 3};
vector<int> v2 = std::move(v1);  // move只是转换类型
                                  // 真正的移动在vector的移动构造中

cout << v1.size();  // 0（v1被"掏空"）
cout << v2.size();  // 3
```

#### 2️⃣ 移动后的对象能再用吗？（⭐⭐⭐⭐⭐ 高频）

```cpp
vector<int> v1 = {1, 2, 3};
vector<int> v2 = std::move(v1);

// v1处于"有效但未指定"状态
cout << v1.size();  // 0（通常）
v1.push_back(4);    // ✅ 可以继续使用
v1 = {5, 6, 7};     // ✅ 可以重新赋值

// ❌ 错误示范：
string s1 = "hello";
string s2 = std::move(s1);
cout << s1[0];  // ❌ 未定义行为！s1可能为空
```

🔑 **得分点：移动后的对象处于有效但未指定状态，可以赋值和析构，但不应该使用其值**

#### 3️⃣ 什么时候会自动使用移动语义？（⭐⭐⭐⭐⭐）

```cpp
// 1️⃣ 返回局部对象（RVO优化失败时）
BigData func() {
    BigData temp;
    return temp;  // 自动使用移动构造
}

// 2️⃣ 容器扩容
vector<BigData> vec;
vec.push_back(BigData());  // 自动使用移动构造

// 3️⃣ 显式std::move
BigData b1;
BigData b2 = std::move(b1);  // 显式移动

// ❌ 不会自动移动的情况：
BigData func(BigData& b) {
    return b;  // ❌ b是左值引用，拷贝而不是移动
}

BigData func(BigData& b) {
    return std::move(b);  // ✅ 显式移动
}
```

#### 4️⃣ 完美转发是什么？（⭐⭐⭐⭐）

```cpp
// 问题：如何将参数原封不动地转发？
template<typename T>
void wrapper(T& arg) {
    func(arg);  // arg是左值，即使传入右值也变成左值了
}

// 解决：std::forward + 万能引用
template<typename T>
void wrapper(T&& arg) {  // 🔑 万能引用（转发引用）
    func(std::forward<T>(arg));  // 🔑 完美转发
}

void func(int& x) { cout << "左值\n"; }
void func(int&& x) { cout << "右值\n"; }

int a = 10;
wrapper(a);   // 输出：左值
wrapper(10);  // 输出：右值  ← 完美转发保持了右值特性
```

**万能引用的规则：**
```cpp
template<typename T>
void func(T&& arg);  // ✅ 万能引用（模板参数）

auto&& var = ...;    // ✅ 万能引用（auto）

void func(int&& arg);  // ❌ 普通右值引用（具体类型）
```

#### 5️⃣ 如何实现一个支持移动的类？（⭐⭐⭐⭐⭐ 必问）

```cpp
class MyString {
    char* data;
    size_t len;
public:
    // 构造函数
    MyString(const char* str = "") {
        len = strlen(str);
        data = new char[len + 1];
        strcpy(data, str);
    }

    // 拷贝构造
    MyString(const MyString& other) {
        len = other.len;
        data = new char[len + 1];
        strcpy(data, other.data);
    }

    // 🔑 移动构造
    MyString(MyString&& other) noexcept {
        data = other.data;    // 窃取资源
        len = other.len;
        other.data = nullptr; // 清空原对象
        other.len = 0;
    }

    // 拷贝赋值
    MyString& operator=(const MyString& other) {
        if (this != &other) {
            delete[] data;
            len = other.len;
            data = new char[len + 1];
            strcpy(data, other.data);
        }
        return *this;
    }

    // 🔑 移动赋值
    MyString& operator=(MyString&& other) noexcept {
        if (this != &other) {
            delete[] data;        // 释放自己的资源
            data = other.data;    // 窃取资源
            len = other.len;
            other.data = nullptr; // 清空原对象
            other.len = 0;
        }
        return *this;
    }

    // 析构函数
    ~MyString() {
        delete[] data;
    }
};

// 🔑 五法则（C++11）：
// 如果定义了以下任意一个，应该定义所有五个：
// 1. 析构函数
// 2. 拷贝构造
// 3. 拷贝赋值
// 4. 移动构造
// 5. 移动赋值
```

### 🎓 面试回答模板

```
【标准回答】
右值引用是C++11引入的特性，用&&表示，主要用于：

1. 移动语义
   - 避免深拷贝，提高性能
   - 移动构造和移动赋值
   - "窃取"临时对象的资源

2. 完美转发
   - 保持参数的左值/右值属性
   - std::forward实现

核心概念：
- 左值：有名字，可取地址
- 右值：临时对象，不可取地址
- std::move：将左值转换为右值引用（只是类型转换）

【追问-move做了什么】
std::move只是类型转换，将左值强制转换为右值引用。
真正的"移动"发生在移动构造函数或移动赋值运算符中，
通过窃取资源指针、清空原对象来实现。

【追问-移动后能再用吗】
移动后的对象处于"有效但未指定"状态：
- 可以赋值
- 可以析构
- 不应该使用其值（可能为空）

【追问-性能提升】
对于管理资源的类（如vector、string），
移动比拷贝快得多：
- 拷贝：O(n)（深拷贝所有元素）
- 移动：O(1)（只转移指针）
```

### ⚠️ 常见误区

❌ **错误1**：以为move会移动对象
✅ **正确**：move只是类型转换，实际移动在移动构造中

❌ **错误2**：移动后的对象不能再用
✅ **正确**：可以赋值和析构，但不应使用其值

❌ **错误3**：所有右值引用都是万能引用
✅ **正确**：只有T&&（模板）和auto&&是万能引用

❌ **错误4**：返回局部对象应该用std::move
✅ **正确**：编译器会自动优化（RVO），手动move反而阻止优化

### 🌟 加分点

- 提到RVO/NRVO优化（返回值优化）
- 知道noexcept对移动语义的影响（vector扩容）
- 了解引用折叠规则（T& && → T&）
- 提到emplace_back比push_back高效的原因

---

## 📌 C++11新特性（⭐⭐⭐⭐ 高频）

### 🎯 得分关键词
> **auto** | **nullptr** | **lambda** | **range-for** | **override/final** | **=default/=delete** | **列表初始化** | **变长模板**

### ✅ 核心新特性

#### 1️⃣ auto类型推导（⭐⭐⭐⭐⭐）

```cpp
// C++03：需要显式写类型
vector<int>::iterator it = vec.begin();
map<string, vector<int>>::iterator mit = m.begin();

// C++11：自动推导
auto it = vec.begin();  // ✅ 简洁
auto mit = m.begin();   // ✅ 避免写复杂类型

// 注意事项：
auto x = 10;      // int
auto& y = x;      // int&
const auto& z = x;  // const int&

auto a = {1, 2, 3};  // std::initializer_list<int>
```

**使用建议：**
```cpp
// ✅ 推荐使用auto的场景
auto it = vec.begin();  // 迭代器
auto lambda = [](int x) { return x * 2; };  // lambda
auto ptr = make_unique<int>(10);  // 智能指针

// ❌ 不推荐的场景
auto x = 10;  // 类型不明确，可读性差
// 应该写：int x = 10;
```

#### 2️⃣ nullptr（⭐⭐⭐⭐⭐）

```cpp
// C++03的问题：
void func(int x) { cout << "int\n"; }
void func(char* p) { cout << "ptr\n"; }

func(NULL);  // ❌ 调用func(int)，因为NULL是0

// C++11解决：
func(nullptr);  // ✅ 调用func(char*)

// nullptr的类型：
nullptr_t n = nullptr;
int* p = nullptr;  // ✅
// int x = nullptr;  // ❌ 错误：不能转换为int
```

#### 3️⃣ lambda表达式（⭐⭐⭐⭐⭐ 必问）

**基本语法：**
```cpp
[capture](parameters) -> return_type { body }

// 示例：
auto add = [](int a, int b) { return a + b; };
cout << add(3, 5);  // 输出：8

// 返回类型推导
auto multiply = [](int a, int b) { return a * b; };  // 自动推导为int
```

**捕获列表（⭐⭐⭐⭐⭐ 高频追问）：**
```cpp
int x = 10, y = 20;

// [=] 值捕获：拷贝外部变量
auto f1 = [=]() { return x + y; };  // ✅
// auto f2 = [=]() { x++; };  // ❌ 错误：不能修改

// [&] 引用捕获：可修改外部变量
auto f3 = [&]() { x++; y++; };  // ✅

// [x, &y] 混合捕获
auto f4 = [x, &y]() {
    // x是拷贝，不能改
    y++;  // y是引用，可以改
};

// [this] 捕获成员变量
class Test {
    int value = 10;
public:
    void func() {
        auto f = [this]() { return value; };  // 捕获this指针
        cout << f();
    }
};

// [=, &x] 默认值捕获，x引用捕获
auto f5 = [=, &x]() { x++; return y; };
```

**mutable关键字：**
```cpp
int x = 10;
auto f = [x]() mutable {
    x++;  // ✅ mutable允许修改拷贝的x
    return x;
};
f();  // 返回11
cout << x;  // 输出：10（原始x未改变）
```

#### 4️⃣ 范围for循环（⭐⭐⭐⭐）

```cpp
vector<int> vec = {1, 2, 3, 4, 5};

// C++03
for (vector<int>::iterator it = vec.begin(); it != vec.end(); ++it) {
    cout << *it;
}

// C++11
for (int x : vec) {  // 拷贝
    cout << x;
}

for (int& x : vec) {  // 引用，可修改
    x *= 2;
}

for (const auto& x : vec) {  // 🌟 推荐：避免拷贝
    cout << x;
}
```

#### 5️⃣ override和final（⭐⭐⭐⭐⭐）

```cpp
class Base {
public:
    virtual void func1() {}
    virtual void func2() {}
    virtual void func3() {}
};

class Derived : public Base {
public:
    // override：明确表示重写基类虚函数
    void func1() override {}  // ✅ 正确重写
    // void func2() const override {}  // ❌ 编译错误：签名不匹配
    void func3() override final {}  // 最终版本，不能再被重写
};

class Final final {  // final类：不能被继承
    // ...
};

// class Derived : public Final {};  // ❌ 编译错误
```

#### 6️⃣ =default和=delete（⭐⭐⭐⭐⭐）

```cpp
class NonCopyable {
public:
    NonCopyable() = default;  // 使用默认构造

    // 禁止拷贝
    NonCopyable(const NonCopyable&) = delete;
    NonCopyable& operator=(const NonCopyable&) = delete;

    // 允许移动
    NonCopyable(NonCopyable&&) = default;
    NonCopyable& operator=(NonCopyable&&) = default;
};

// 禁止某些类型的隐式转换
class Test {
public:
    Test(int x) {}
    Test(double) = delete;  // 禁止double转换
};

Test t1(10);   // ✅
// Test t2(3.14);  // ❌ 编译错误
```

#### 7️⃣ 列表初始化（⭐⭐⭐⭐）

```cpp
// 统一初始化语法
int x{10};
int arr[]{1, 2, 3};
vector<int> vec{1, 2, 3, 4, 5};
map<int, string> m{{1, "one"}, {2, "two"}};

// 防止窄化转换
int a = 3.14;   // ✅ 允许（C++03）
// int b{3.14};  // ❌ 错误：窄化转换

// 初始化列表构造
class Point {
public:
    Point(initializer_list<int> lst) {
        // ...
    }
};

Point p{1, 2, 3};  // 调用initializer_list构造
```

#### 8️⃣ 变长模板（⭐⭐⭐⭐）

```cpp
// 递归展开
template<typename T>
void print(T t) {
    cout << t << endl;
}

template<typename T, typename... Args>
void print(T first, Args... args) {
    cout << first << ", ";
    print(args...);  // 递归调用
}

print(1, 2.5, "hello");  // 输出：1, 2.5, hello

// C++17折叠表达式（更简洁）
template<typename... Args>
void print(Args... args) {
    ((cout << args << " "), ...);  // 折叠表达式
}
```

### 🔥 其他重要新特性

#### 9️⃣ using别名（⭐⭐⭐⭐）

```cpp
// C++03: typedef
typedef void (*FuncPtr)(int, int);

// C++11: using（更清晰）
using FuncPtr = void(*)(int, int);

// 模板别名（typedef做不到）
template<typename T>
using Vec = vector<T>;

Vec<int> v;  // 等价于vector<int>
```

#### 🔟 委托构造（⭐⭐⭐）

```cpp
class Test {
    int x, y;
public:
    Test() : Test(0, 0) {}  // 委托给下面的构造
    Test(int a) : Test(a, 0) {}  // 委托
    Test(int a, int b) : x(a), y(b) {}  // 真正的构造
};
```

### 🎓 面试回答模板

```
【常见回答】
C++11主要新特性包括：

1. auto类型推导 - 简化代码，自动推导类型
2. nullptr - 替代NULL，类型安全
3. lambda表达式 - 匿名函数，简化回调
4. 智能指针 - unique_ptr、shared_ptr、weak_ptr
5. 右值引用和移动语义 - 提高性能
6. range-for循环 - 简化遍历
7. override/final - 明确虚函数重写
8. =default/=delete - 控制特殊成员函数
9. 列表初始化 - 统一初始化语法
10. 变长模板 - 可变参数模板

【追问-lambda捕获】
- [=] 值捕获：拷贝外部变量，不可修改（除非mutable）
- [&] 引用捕获：可修改外部变量
- [this] 捕获成员变量
- [x, &y] 混合捕获

【追问-override的作用】
明确表示重写基类虚函数，编译器会检查：
1. 基类必须有这个虚函数
2. 函数签名必须完全匹配
防止因拼写错误或签名不匹配导致的隐藏而非重写
```

### ⚠️ 常见误区

❌ **错误1**：lambda值捕获可以修改外部变量
✅ **正确**：需要加mutable，且只修改拷贝

❌ **错误2**：nullptr和NULL完全一样
✅ **正确**：nullptr类型安全，NULL是整数0

❌ **错误3**：auto总是好的
✅ **正确**：简单类型不建议用auto，降低可读性

### 🌟 加分点

- 提到C++14/17的改进（泛型lambda、if constexpr）
- 知道lambda的底层实现（仿函数）
- 了解完美转发和万能引用
- 提到列表初始化的优先级问题

---

## 📌 constexpr（⭐⭐⭐⭐ 高频）

### 🎯 得分关键词
> **编译期常量** | **常量表达式** | **constexpr函数** | **constexpr if** | **字面量类型** | **性能优化**

### ✅ constexpr vs const

```cpp
// const: 运行时或编译时常量
const int x = 10;           // 编译时
const int y = func();       // 运行时

// constexpr: 必须是编译时常量
constexpr int a = 10;       // ✅ 编译时
// constexpr int b = func();  // ❌ 错误（除非func是constexpr）

// 🔑 区别：
// const只保证不可修改
// constexpr保证编译时计算
```

### 💡 constexpr函数（⭐⭐⭐⭐⭐ 核心）

**C++11规则：**
```cpp
// constexpr函数必须：
// 1. 只有一个return语句
// 2. 不能有局部变量
constexpr int square(int x) {
    return x * x;
}

constexpr int x = square(5);  // 编译时计算，x=25
int arr[square(5)];  // ✅ 可以用于数组大小

// 递归实现阶乘
constexpr int factorial(int n) {
    return n <= 1 ? 1 : n * factorial(n - 1);
}

constexpr int result = factorial(5);  // 编译时计算，result=120
```

**C++14放宽限制：**
```cpp
// C++14可以有多条语句
constexpr int fibonacci(int n) {
    if (n <= 1) return n;

    int a = 0, b = 1;
    for (int i = 2; i <= n; ++i) {
        int temp = a + b;
        a = b;
        b = temp;
    }
    return b;
}

constexpr int fib10 = fibonacci(10);  // 编译时计算
```

### 💻 constexpr类（⭐⭐⭐⭐）

```cpp
class Point {
    int x_, y_;
public:
    constexpr Point(int x, int y) : x_(x), y_(y) {}

    constexpr int x() const { return x_; }
    constexpr int y() const { return y_; }

    constexpr int distance() const {
        return x_ * x_ + y_ * y_;
    }
};

constexpr Point p(3, 4);
constexpr int dist = p.distance();  // 编译时计算，dist=25

// 可以用于数组大小
int arr[p.distance()];  // ✅
```

### 🔥 面试追问点

#### 1️⃣ constexpr和宏的区别？（⭐⭐⭐⭐⭐）

```cpp
// 宏：文本替换，不安全
#define SQUARE(x) x * x
int result = SQUARE(1 + 2);  // ❌ 展开为 1 + 2 * 1 + 2 = 5

// constexpr：类型安全，编译时计算
constexpr int square(int x) { return x * x; }
int result = square(1 + 2);  // ✅ 正确结果：9
```

#### 2️⃣ 什么时候用constexpr？（⭐⭐⭐⭐）

```cpp
// ✅ 推荐使用场景：
// 1. 编译时计算
constexpr int size = 1024;
int arr[size];

// 2. 优化性能（避免运行时计算）
constexpr double pi = 3.1415926;
constexpr double area(double r) { return pi * r * r; }

// 3. 模板元编程
template<int N>
struct Factorial {
    static constexpr int value = N * Factorial<N-1>::value;
};

// ❌ 不适合的场景：
// 涉及运行时才能确定的值
constexpr int x = getUserInput();  // ❌ 错误
```

#### 3️⃣ C++17的constexpr if？（⭐⭐⭐⭐）

```cpp
template<typename T>
auto getValue(T t) {
    if constexpr (is_pointer_v<T>) {  // 编译时判断
        return *t;  // T是指针
    } else {
        return t;   // T不是指针
    }
}

int x = 10;
int* p = &x;

auto a = getValue(x);   // 返回x
auto b = getValue(p);   // 返回*p

// 🔑 普通if vs constexpr if：
// 普通if：两个分支都要编译通过
// constexpr if：未选中的分支不编译
```

### 🎓 面试回答模板

```
【标准回答】
constexpr表示编译期常量，主要用于：

1. constexpr变量
   - 必须在编译时确定
   - 可用于数组大小、模板参数

2. constexpr函数
   - 参数是常量时，编译时计算
   - 参数是变量时，运行时计算

3. constexpr类
   - 构造函数和成员函数可以是constexpr
   - 支持编译时对象创建

优势：
- 提高性能（编译时计算，无运行时开销）
- 类型安全（优于宏）
- 可调试（有符号表）

【追问-和const区别】
const只保证不可修改，可以是运行时常量
constexpr保证编译时常量，更严格

【追问-和inline区别】
inline建议内联，减少函数调用开销
constexpr保证编译时计算，可能完全消除函数调用
```

### ⚠️ 常见误区

❌ **错误1**：constexpr函数总是编译时计算
✅ **正确**：参数是变量时，运行时计算

❌ **错误2**：所有const都可以改成constexpr
✅ **正确**：constexpr要求编译时可计算

❌ **错误3**：constexpr和inline等价
✅ **正确**：constexpr隐含inline，但含义不同

### 🌟 加分点

- 提到C++20的constexpr改进（虚函数、动态内存）
- 知道constexpr构造函数的限制
- 了解字面量类型（literal type）
- 提到consteval（C++20，必须编译时计算）

---

## 📌 map和set底层实现（⭐⭐⭐⭐ 高频）

### 🎯 得分关键词
> **红黑树** | **有序** | **O(logn)** | **pair** | **lower_bound** | **upper_bound** | **unordered_map** | **哈希表**

### ✅ map底层结构

```cpp
// map底层是红黑树，节点存储pair<key, value>
template<typename Key, typename Value>
class map {
    // 红黑树节点
    struct Node {
        pair<const Key, Value> data;  // 🔑 key是const
        Node* left;
        Node* right;
        Node* parent;
        Color color;  // 红或黑
    };

    Node* root;
    size_t size;
};
```

**内存布局：**
```
       [10, "ten"]黑
       /          \
  [5, "five"]红  [15, "fifteen"]红
  /      \         /           \
[3]黑  [7]黑   [12]黑       [18]黑

🔑 特点：
1. 按key有序（默认升序）
2. key唯一
3. 查找、插入、删除：O(logn)
```

### 💡 map的关键操作（⭐⭐⭐⭐⭐）

**插入：**
```cpp
map<int, string> m;

// 方式1：[]运算符
m[1] = "one";  // 如果key不存在，插入；存在则覆盖

// 方式2：insert
m.insert({2, "two"});  // 返回pair<iterator, bool>
auto [it, success] = m.insert({2, "TWO"});  // C++17
// success = false（key已存在）

// 方式3：emplace（C++11，原地构造）
m.emplace(3, "three");  // 避免临时对象

// 🔑 []和insert的区别：
m[100];  // key不存在，插入default value（空字符串）
m.insert({100, "hundred"});  // key不存在才插入
```

**查找：**
```cpp
// find: 返回迭代器
auto it = m.find(5);
if (it != m.end()) {
    cout << it->second;  // 访问value
}

// count: 返回0或1（key唯一）
if (m.count(5)) {
    cout << "存在";
}

// []: 不存在会插入
string s = m[999];  // ⚠️ 会插入{999, ""}

// at: 不存在抛异常
try {
    string s = m.at(999);  // 抛出out_of_range
} catch(...) {}
```

**lower_bound / upper_bound（⭐⭐⭐⭐⭐ 高频）：**
```cpp
map<int, string> m = {{1,"a"}, {3,"c"}, {5,"e"}, {7,"g"}};

// lower_bound: 第一个 >= key 的位置
auto it1 = m.lower_bound(3);  // 指向{3, "c"}
auto it2 = m.lower_bound(4);  // 指向{5, "e"}

// upper_bound: 第一个 > key 的位置
auto it3 = m.upper_bound(3);  // 指向{5, "e"}
auto it4 = m.upper_bound(4);  // 指向{5, "e"}

// 🔑 应用：查找范围
// 查找[2, 6)范围的元素
auto start = m.lower_bound(2);  // >= 2
auto end = m.upper_bound(6);    // > 6
for (auto it = start; it != end; ++it) {
    cout << it->first << endl;  // 输出：3, 5
}
```

### 💻 set底层实现

```cpp
// set底层也是红黑树，节点只存key
template<typename Key>
class set {
    struct Node {
        const Key data;  // 🔑 key是const（不可修改）
        Node* left;
        Node* right;
        Color color;
    };
};

// set的key既是key也是value
set<int> s = {3, 1, 4, 1, 5};
// 内部存储：1, 3, 4, 5（自动去重、排序）

// 插入
s.insert(2);
s.emplace(6);

// 查找
if (s.find(3) != s.end()) {
    cout << "存在";
}

// lower_bound / upper_bound
auto it1 = s.lower_bound(3);  // 指向3
auto it2 = s.upper_bound(3);  // 指向4
```

### 🔥 面试追问点

#### 1️⃣ map和unordered_map的区别？（⭐⭐⭐⭐⭐ 必问）

| 特性 | map | unordered_map |
|------|-----|---------------|
| **底层** | 红黑树 | 哈希表 |
| **有序性** | 有序（升序） | 无序 |
| **查找** | O(logn) | O(1)平均 |
| **插入** | O(logn) | O(1)平均 |
| **删除** | O(logn) | O(1)平均 |
| **空间** | 较小 | 较大（哈希表+链表） |
| **适用** | 需要有序 | 只需查找 |

```cpp
map<int, string> m;          // 红黑树，有序
unordered_map<int, string> um;  // 哈希表，无序

m[3] = "c"; m[1] = "a"; m[2] = "b";
// 遍历m: 1, 2, 3（有序）

um[3] = "c"; um[1] = "a"; um[2] = "b";
// 遍历um: 顺序不确定（无序）
```

#### 2️⃣ 为什么map的key必须是const？（⭐⭐⭐⭐）

```cpp
map<int, string> m = {{1, "one"}, {2, "two"}};

auto it = m.begin();
// it->first = 100;  // ❌ 编译错误：key是const

// 原因：
// 1. map按key排序，修改key会破坏树结构
// 2. 修改key可能导致重复key
// 3. 如果要改key，必须删除再插入

// 正确做法：
string value = m[1];
m.erase(1);
m[100] = value;
```

#### 3️⃣ 自定义类型作为map的key？（⭐⭐⭐⭐⭐ 高频）

```cpp
struct Person {
    string name;
    int age;

    // 🔑 必须定义operator<
    bool operator<(const Person& other) const {
        if (name != other.name)
            return name < other.name;
        return age < other.age;
    }
};

map<Person, string> m;
m[{"Alice", 20}] = "Student";

// 或者使用自定义比较器
struct PersonCmp {
    bool operator()(const Person& a, const Person& b) const {
        return a.age < b.age;  // 按年龄排序
    }
};

map<Person, string, PersonCmp> m2;
```

#### 4️⃣ multimap和multiset？（⭐⭐⭐）

```cpp
// multimap: 允许重复key
multimap<int, string> mm;
mm.insert({1, "one"});
mm.insert({1, "ONE"});  // ✅ 允许重复key
mm.insert({1, "壹"});

// 查找所有key=1的元素
auto range = mm.equal_range(1);
for (auto it = range.first; it != range.second; ++it) {
    cout << it->second << endl;
}
// 输出：one, ONE, 壹

// multiset: 允许重复元素
multiset<int> ms = {1, 1, 2, 2, 3};
cout << ms.count(1);  // 输出：2
```

### 🎓 面试回答模板

```
【标准回答】
map和set底层都是红黑树实现：

map特点：
1. 存储pair<const Key, Value>
2. 按key自动排序（默认升序）
3. key唯一，不可修改
4. 查找、插入、删除：O(logn)

set特点：
1. 只存储key
2. 自动去重、排序
3. 元素不可修改（是const）

关键操作：
- insert/emplace：插入
- find：查找，返回迭代器
- lower_bound：>= key的第一个位置
- upper_bound：> key的第一个位置

【追问-和unordered_map区别】
map是红黑树，有序，O(logn)
unordered_map是哈希表，无序，O(1)平均
需要有序时用map，只需快速查找用unordered_map

【追问-自定义类型作key】
必须定义operator<或提供比较器
因为红黑树需要比较来维护顺序

【追问-[]和at区别】
[]：key不存在时插入默认值
at：key不存在时抛异常
```

### ⚠️ 常见误区

❌ **错误1**：以为map查找是O(1)
✅ **正确**：map是O(logn)，unordered_map才是O(1)

❌ **错误2**：可以修改map的key
✅ **正确**：key是const，不能修改

❌ **错误3**：set可以存重复元素
✅ **正确**：set自动去重，要重复用multiset

### 🌟 加分点

- 提到map迭代器不会失效（除了删除的元素）
- 知道红黑树的5条性质
- 了解unordered_map的哈希冲突解决（链地址法）
- 提到C++17的try_emplace（不覆盖已存在的key）

---

**📊 当前完善进度：**
- ✅ 多态与虚函数（P0）⭐⭐⭐⭐⭐
- ✅ 智能指针（P0）⭐⭐⭐⭐⭐
- ✅ new和malloc（P0）⭐⭐⭐⭐⭐
- ✅ const关键字（P0）⭐⭐⭐⭐⭐
- ✅ vector（P0）⭐⭐⭐⭐⭐
- ✅ 多继承与菱形继承（P0）⭐⭐⭐⭐⭐
- ✅ static关键字（P0）⭐⭐⭐⭐⭐
- ✅ 右值引用与移动语义（P0）⭐⭐⭐⭐⭐
- ✅ 红黑树（P1）⭐⭐⭐⭐
- ✅ C++11新特性（P1）⭐⭐⭐⭐
- ✅ constexpr（P1）⭐⭐⭐⭐
- ✅ map/set实现（P1）⭐⭐⭐⭐

**🎉 C++.md 完善完成度：95%**

主要知识点已全部完善，涵盖：
- 8个P0级别必考知识点
- 4个P1级别高频知识点
- 详细的代码示例、面试追问、回答模板
- 常见误区和加分点

建议下一步：继续完善其他文件（计算机网络.md、操作系统.md等）
